import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const assignmentMarkSlice = createSlice({
  name: "assignmentMark",
  initialState,
  reducers: {},
});

export const {} = assignmentMarkSlice.actions;
export default assignmentMarkSlice.reducer;
